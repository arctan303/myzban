version: '3.8'

services:
  panel:
    build: .
    container_name: pnm-panel
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - panel-data:/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/panel.db

volumes:
  panel-data:
